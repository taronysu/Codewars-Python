https://www.codewars.com/kata/5526fc09a1bbd946250002dc/train/python

def find_outlier(integers):
    odd = [el  for el in integers if el%2!=0]
    even = [el for el in integers if el%2==0]
    return odd[0] if len(odd) == 1 else even[0]
