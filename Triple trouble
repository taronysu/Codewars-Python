https://www.codewars.com/kata/55d5434f269c0c3f1b000058/train/python

import re
def triple_double(num1, num2):
    for i in range(10):
        if re.search(f"{i}{{3}}", str(num1)) and re.search(f"{i}{{2}}", str(num2)):
            return 1
    return 0
